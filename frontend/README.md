# ChampManager Web

Frontend React do ChampManager.

```bash
copy .env.example .env
npm install
npm run dev
```

Estrutura: `src/app` contém providers e rotas; `src/pages`, composições de páginas; `src/features`, domínios; e `src/shared`, infraestrutura e componentes compartilhados.
